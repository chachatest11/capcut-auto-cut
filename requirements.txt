# AutoCut — cross-platform (Windows + macOS incl. Apple Silicon)
# Install with:  pip install -r requirements.txt
# pip auto-selects the correct wheel for your OS/CPU.

fastapi>=0.138
uvicorn>=0.49
python-multipart>=0.0.32

# audio/video decode (bundles ffmpeg libs — no system ffmpeg needed)
av>=17.1
numpy>=2.0
opencv-python>=4.13

# speech: ASR + voice activity detection
faster-whisper>=1.2
silero-vad>=6.2
torch>=2.4

# highlight analysis (Gemini) + YouTube source
google-genai>=2.10
yt-dlp>=2025.1
